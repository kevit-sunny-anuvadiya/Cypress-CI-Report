import { Route } from '@angular/router';
import { InvalidTokenComponent } from './invalid-token.component';

export const invalidTokenRoutes: Route[] = [
    {
        path     : '',
        component: InvalidTokenComponent
    }
];
