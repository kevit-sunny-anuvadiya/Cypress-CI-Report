import { Route } from '@angular/router';
import { RevokeAccessComponent } from './revoke-access.component';

export const revokeAccessRoutes: Route[] = [
    {
        path: '',
        component:RevokeAccessComponent
    }
];
